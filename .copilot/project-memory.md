# Project Memory

La memoria activa del proyecto ha sido migrada a Claude Code:

- **Arquitectura, comandos y convenciones** → `CLAUDE.md` en la raíz del repo
- **Estado actual, decisiones técnicas, prioridades y perfil de trabajo** → memoria de Claude Code (`~/.claude/projects/.../memory/`)

Este fichero se mantiene como referencia histórica. Para retomar el proyecto con Claude Code, la memoria se carga automáticamente al iniciar una sesión en este directorio.
