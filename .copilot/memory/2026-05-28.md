- Se unificó de verdad el ciclo de tabs entre detalle de partido y detalle de equipo, evitando que el detalle de equipo rerenderizara siempre toda la vista. El detalle de equipo ahora mantiene la botonera estable y hace swap del panel de contenido activo.
- El bug escurridizo de los filtros del resumen del detalle de equipo estaba en el subview compartido: los botones `data-team-filter` no quedaban enlazados en la carga inicial, solo después de un cambio de tab. Se corrigió forzando `renderTeamContentOnly()` también al entrar por primera vez, para que el bind inicial de filtros exista desde el principio.
- También se dejó corregido el flujo de filtros del resumen para que al pulsarlos cambien a la tab `partidos`, activen la pill correspondiente y rendericen el contenido filtrado en el siguiente frame.
- El componente compartido de tabs quedó más limpio con helpers de animación en `uiTabs.js` y tokens CSS `--tab-pill-*` en `components-partido-detalle.css`.
